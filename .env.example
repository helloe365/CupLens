# Copy this file to .env for local use. Keep real credentials out of Git.
# Backend only: never expose this variable through frontend code or API responses.
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://llm-0nsl26x7l7v72b8i.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen3.7-plus
MODEL_VARIANT=dixon-coles-lightgbm-meta

# Fail-closed result updater. The source must return the verified JSON bundle
# contract used by scripts/fetch_current_results.py (not an arbitrary web page).
# Enabling still requires a snapshot ID, cutoff, and manual verification.
AUTO_UPDATE_ENABLED=false
AUTO_UPDATE_SOURCE_URL=
